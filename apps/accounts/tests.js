from django.test import TestCase
const res = pm.response.json();
pm.collectionVariables.set("reenu_token", res.tokens.access);

const res = pm.response.json();
pm.collectionVariables.set("reenu_token", res.tokens.access);